<!DOCTYPE html>
<html>
    <head>
        <script src="/javascripts/jquery-3.5.1.min.js"></script>
        <title><%= title %></title>
        <link rel='stylesheet' href='/stylesheets/style.css'/>
    </head>
    <body>
        <h1><%= title %></h1>
            <table border="1">
                <tr>
                    <td>유저 이름</td>
                    <td>메뉴 이름</td>
                    <td>가격</td>
                    <td>수량</td>
                    <td>장바구니에서 빼기</td>
                </tr>
                <%
                var price = 0;
                for(var i=0; i<rows.length; i++){
                    var oneItem = rows[i];
                    price = price + rows[i].menu_price * rows[i].menu_cnt;
                %>
                    <tr>
                        <td><%=oneItem.user_name%></td>
                        <td><%=oneItem.menu_name%></td>
                        <td><%=oneItem.menu_price%></td>
                        <td><%=oneItem.menu_cnt%></td>
                        <td><a href="/buyer/delete-cart/<%=oneItem.store_ID%>/<%=oneItem.menuID%>">장바구니에서 빼기</a></td>
                    </tr>
<%        
                }
%>
            </table>
            <br>
            <table border="1">
                <tr>
                    <td>총 가격</td>
                    <td>구매</td>
                </tr>
                    <tr>
                        <td><%=price%></td>
                        <td><a href="/buyer/buy/<%=rows[0].userID%>">구매</a></td>
                    </tr>
            </table>
            
            <a href="/buyer/print-menu">메뉴 보러가기</a>
            <a href="/buyer">돌아가기</a>
</html>